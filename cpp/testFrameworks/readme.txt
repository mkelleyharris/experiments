Simple Test examples to work out file structure and configuration
with various test frameworks.

You can use the makefile to build and run the tests, via:
make test

NOTE: edit the include path to get to the Boost directory on your system.
This uses the Boost headers only. It would speed things up to use the Boost 
library, but this demo is easier to install and run.
